# 📦 Map Analyzer - 全プラットフォーム向けインストーラー作成ガイド (日本語)

## 🌍 はじめに

このガイドでは、Map Analyzerの**Windows、macOS、Linux**向けインストーラーを作成する方法を日本語でわかりやすく説明します。

---

## 📋 対応プラットフォーム一覧

| OS | インストーラー | ビルド環境 | サイズ | 配布方法 |
|----|---------------|-----------|--------|---------|
| 🪟 Windows | `.exe` | Windows PC | 25-30MB | GitHub/直接配布 |
| 🍎 macOS | `.dmg` | Mac | 20-25MB | GitHub/直接配布 |
| 🐧 Linux | `.AppImage` | Linux PC | 23-28MB | GitHub/直接配布 |
| 🐧 Linux | `.deb` | Linux PC | 23-28MB | apt/GitHub |
| 🐧 Linux | `.snap` | Linux PC | 30-35MB | Snap Store |

---

## 🪟 Windows インストーラー (.exe)

### 必要なもの
- Windows 10以上のPC
- Flutter SDK 3.35.4以上
- Visual Studio 2022 (C++開発)
- Inno Setup 6.x (無料)

### 作成手順 (5ステップ)

#### ステップ1: Flutterアプリをビルド
```powershell
cd C:\path\to\flutter_app
flutter pub get
flutter build windows --release
```

#### ステップ2: Inno Setupをインストール
https://jrsoftware.org/isdl.php からダウンロード・インストール

#### ステップ3: インストーラーをビルド

**GUI (簡単):**
1. Inno Setup Compiler起動
2. `installer\windows_installer.iss` を開く
3. `Build` → `Compile` (F9キー)

**CLI:**
```powershell
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installer\windows_installer.iss"
```

#### ステップ4: 完成!
```
出力先: installer\output\MapAnalyzer_Setup_v1.0.0_x64.exe
```

### 詳細ガイド
📖 [インストーラー作成手順_日本語.md](インストーラー作成手順_日本語.md) - 詳しい日本語ガイド

---

## 🍎 macOS インストーラー (.dmg)

### 必要なもの
- macOS 12 (Monterey)以上
- Flutter SDK 3.35.4以上
- Xcode 14以上
- Homebrew

### 作成手順 (4ステップ)

#### ステップ1: create-dmgをインストール
```bash
brew install create-dmg
```

#### ステップ2: Flutterアプリをビルド
```bash
cd /path/to/flutter_app
flutter pub get
flutter build macos --release
```

#### ステップ3: DMGインストーラーを作成
```bash
cd installer/macos
chmod +x create_dmg.sh
./create_dmg.sh
```

#### ステップ4: 完成!
```
出力先: installer/macos/output/MapAnalyzer_v1.0.0_macOS.dmg
```

### ユーザー向けインストール方法
1. DMGファイルをダブルクリック
2. `Map Analyzer.app` を `Applications` フォルダにドラッグ
3. DMGをイジェクト
4. Launchpadから起動

### DMGの中身
- アプリケーションバンドル (`Map Analyzer.app`)
- Applicationsフォルダへのショートカット
- カスタム背景画像 (オプション)

---

## 🐧 Linux インストーラー (3種類)

### 共通の必要なもの
- Ubuntu 20.04+ / Debian 11+ / その他Linux
- Flutter SDK 3.35.4以上
- ビルドツール

```bash
sudo apt-get update
sudo apt-get install -y cmake ninja-build clang libgtk-3-dev liblzma-dev libglu1-mesa-dev
```

---

### 📦 形式1: AppImage (最も簡単!)

**おすすめポイント:**
- ✅ インストール不要、ダウンロードして即実行
- ✅ すべてのLinuxディストリビューションで動作
- ✅ 管理者権限不要

#### 作成手順

```bash
# 1. Flutterアプリをビルド
cd /path/to/flutter_app
flutter pub get
flutter build linux --release

# 2. AppImageを作成
cd installer/linux
chmod +x create_appimage.sh
./create_appimage.sh
```

#### 完成!
```
出力先: installer/linux/output/MapAnalyzer-1.0.0-x86_64.AppImage
```

#### ユーザー向け使用方法
```bash
# 実行権限を付与
chmod +x MapAnalyzer-1.0.0-x86_64.AppImage

# 実行
./MapAnalyzer-1.0.0-x86_64.AppImage
```

#### デスクトップ統合 (オプション)
- AppImageLauncherをインストール
- AppImageファイルをダブルクリック
- 「統合して実行」を選択

---

### 📦 形式2: Debian Package (.deb)

**おすすめポイント:**
- ✅ Ubuntu/Debian標準のパッケージ
- ✅ `apt`コマンドで管理可能
- ✅ 依存関係を自動解決
- ✅ システムメニューに自動登録

#### 作成手順

```bash
# 1. Flutterアプリをビルド
cd /path/to/flutter_app
flutter pub get
flutter build linux --release

# 2. .debパッケージを作成
cd installer/linux
chmod +x create_deb.sh
./create_deb.sh
```

#### 完成!
```
出力先: installer/linux/output/map-analyzer_1.0.0_amd64.deb
```

#### ユーザー向けインストール方法

**方法1: GUIでインストール**
1. .debファイルをダブルクリック
2. 「ソフトウェアのインストール」をクリック
3. パスワードを入力

**方法2: コマンドラインでインストール**
```bash
sudo dpkg -i map-analyzer_1.0.0_amd64.deb
sudo apt-get install -f  # 依存関係を自動修正
```

#### 起動方法
```bash
# コマンドラインから
map-analyzer

# または、アプリケーションメニューから「Map Analyzer」を選択
```

#### アンインストール
```bash
sudo apt-get remove map-analyzer
```

---

### 📦 形式3: Snap Package

**おすすめポイント:**
- ✅ Ubuntu App Storeで配信可能
- ✅ 自動更新
- ✅ サンドボックスで安全
- ✅ 複数バージョンの共存可能

#### 作成手順

```bash
# 1. snapcraftをインストール
sudo snap install snapcraft --classic

# 2. Snapパッケージをビルド
cd /path/to/flutter_app

# snapcraft.yamlをコピー
cp installer/linux/create_snap.yaml snapcraft.yaml

# ビルド実行 (15-25分かかります)
snapcraft
```

#### 完成!
```
出力先: map-analyzer_1.0.0_amd64.snap
```

#### ユーザー向けインストール方法

**ローカルインストール:**
```bash
sudo snap install map-analyzer_1.0.0_amd64.snap --dangerous
```

**Snap Storeから (公開後):**
```bash
sudo snap install map-analyzer
```

#### Snap Store公開 (オプション)
```bash
snapcraft login
snapcraft upload map-analyzer_1.0.0_amd64.snap
snapcraft release map-analyzer 1.0.0 stable
```

---

## 🔧 トラブルシューティング

### Windows

#### ❌ "Source file does not exist" エラー
**原因:** Flutterビルドが完了していない

**解決:**
```powershell
flutter clean
flutter pub get
flutter build windows --release
```

#### ❌ アプリが起動しない
**原因:** Visual C++ Redistributableが未インストール

**解決:** https://aka.ms/vs/17/release/vc_redist.x64.exe をインストール

---

### macOS

#### ❌ "create-dmg: command not found"
**解決:**
```bash
brew install create-dmg
```

#### ❌ "App is damaged and can't be opened"
**原因:** macOSのセキュリティ制限

**解決:**
```bash
xattr -cr /Applications/Map\ Analyzer.app
```

または、システム環境設定 → セキュリティとプライバシー → 「このまま開く」

---

### Linux

#### ❌ "appimagetool not found"
**解決:** スクリプトが自動ダウンロードします。手動の場合:
```bash
wget https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
chmod +x appimagetool-x86_64.AppImage
sudo mv appimagetool-x86_64.AppImage /usr/local/bin/appimagetool
```

#### ❌ "GTK not found" エラー
**解決:**
```bash
sudo apt-get install libgtk-3-dev
```

---

## 📊 ビルド時間とサイズ比較

| プラットフォーム | 形式 | ビルド時間 | サイズ | インストール時間 |
|-----------------|------|-----------|--------|----------------|
| Windows | `.exe` | 10-15分 | 25-30MB | 30秒-1分 |
| macOS | `.dmg` | 8-12分 | 20-25MB | 10-20秒 |
| Linux | `.AppImage` | 7-10分 | 23-28MB | 即座 |
| Linux | `.deb` | 5-8分 | 23-28MB | 10-20秒 |
| Linux | `.snap` | 15-25分 | 30-35MB | 1-2分 |

---

## 🎯 どのインストーラーを使うべきか?

### エンドユーザー向け推奨

#### Windows
✅ **`.exe` (Inno Setup)** 一択
- ワンクリックインストール
- スタートメニュー登録
- 簡単アンインストール

#### macOS
✅ **`.dmg`** 一択
- macOS標準の配布形式
- ドラッグ&ドロップでインストール
- 署名なしでも配布可能

#### Linux
**初心者向け:**
✅ **`.AppImage`** (最も簡単)
- インストール不要
- ダウンロードして即実行
- 管理者権限不要

**上級者向け:**
✅ **`.deb`** (Ubuntu/Debian標準)
- システム統合
- パッケージ管理
- 自動更新準備

**アプリストア配信:**
✅ **`.snap`** (Ubuntu App Store)
- 自動更新
- サンドボックス
- トランザクショナル更新

---

## 📦 すべてをまとめてビルド (開発者向け)

### ビルドスクリプト例

```bash
#!/bin/bash
# すべてのプラットフォーム向けインストーラーをビルド

# Windowsビルド (Windows PCで実行)
# cd /path/to/flutter_app
# flutter build windows --release
# & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installer\windows_installer.iss"

# macOSビルド (Macで実行)
flutter build macos --release
cd installer/macos && ./create_dmg.sh && cd ../..

# Linuxビルド (Linux PCで実行)
flutter build linux --release
cd installer/linux
./create_appimage.sh
./create_deb.sh
# snapcraft  # オプション
cd ../..

echo "すべてのインストーラーをビルドしました!"
ls -lh installer/output/
ls -lh installer/macos/output/
ls -lh installer/linux/output/
```

---

## 🚀 GitHub Releasesへアップロード

```bash
# すべてのインストーラーをGitHub Releaseに公開
gh release create v1.0.0 \
  installer/output/MapAnalyzer_Setup_v1.0.0_x64.exe \
  installer/macos/output/MapAnalyzer_v1.0.0_macOS.dmg \
  installer/linux/output/MapAnalyzer-1.0.0-x86_64.AppImage \
  installer/linux/output/map-analyzer_1.0.0_amd64.deb \
  --title "Map Analyzer v1.0.0 - 全プラットフォーム対応" \
  --notes "Windows、macOS、Linux向けインストーラーをすべて同梱しています。"
```

---

## ✅ 最終チェックリスト

### ビルド前
- [ ] Flutter SDK 3.35.4以上インストール済み
- [ ] 各プラットフォームのビルドツールインストール済み
- [ ] `flutter doctor` ですべてOK
- [ ] プロジェクトをGitHubにプッシュ済み

### ビルド中
- [ ] Windows: `.exe` ビルド成功
- [ ] macOS: `.dmg` ビルド成功
- [ ] Linux: `.AppImage` と `.deb` ビルド成功

### テスト
- [ ] 各インストーラーが正常に動作
- [ ] アプリが起動し、APIキー設定画面が表示される
- [ ] Mapillary検索が動作する
- [ ] データエクスポートが動作する
- [ ] アンインストールが正常に完了

### 配布
- [ ] ファイル名にバージョン番号が含まれている
- [ ] ファイルサイズが想定範囲内
- [ ] ウイルススキャン完了
- [ ] GitHub Releasesにアップロード完了
- [ ] README.mdのダウンロードリンク更新

---

## 📚 詳細ドキュメント

### Windows
- 📖 [インストーラー作成手順_日本語.md](インストーラー作成手順_日本語.md) - 日本語完全ガイド
- 📖 [BUILD_INSTALLER.md](BUILD_INSTALLER.md) - 英語詳細ガイド
- 📖 [INSTALLER_FAQ.md](INSTALLER_FAQ.md) - よくある質問

### macOS/Linux
- 📖 [BUILD_ALL_PLATFORMS.md](BUILD_ALL_PLATFORMS.md) - 英語全プラットフォームガイド
- 📖 [macos/create_dmg.sh](macos/create_dmg.sh) - macOSビルドスクリプト
- 📖 [linux/create_appimage.sh](linux/create_appimage.sh) - AppImageビルドスクリプト
- 📖 [linux/create_deb.sh](linux/create_deb.sh) - DEBビルドスクリプト

---

## 💡 プロのヒント

1. **ビルド環境を別々に準備**
   - Windows専用PC、Mac専用、Linux専用を用意
   - または、仮想マシンやCI/CDを活用

2. **バージョン管理**
   - Gitタグでバージョン管理: `git tag v1.0.0`
   - リリースノートを必ず記載

3. **テスト環境**
   - クリーンな環境でインストールテスト
   - 仮想マシンやDockerコンテナを活用

4. **自動化**
   - GitHub Actionsで自動ビルド
   - プルリクエストごとにテストビルド

---

## 🎉 完成!

すべてのプラットフォーム向けインストーラーが完成しました!

**Map Analyzer v1.0.0**
© 2025 MapAnalyzer Project

ユーザーが自分のOSに合ったインストーラーを選んでダウンロードできます!

---

**GitHub:** https://github.com/geomatsuyama/Maptag
**Web版:** https://5060-i6w1gve4ssf8ly2hkqauq-02b9cc79.sandbox.novita.ai
